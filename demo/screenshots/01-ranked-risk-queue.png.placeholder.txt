PLACEHOLDER — replace with a real screenshot before submission.

To capture this screenshot:
1. Launch: python -m streamlit run src/app.py --server.port 8502
2. Open http://localhost:8502
3. Tab 1 (Ranked Asset Priority Queue) should be visible by default
4. Take a full-page browser screenshot showing:
   - At least the top 10 rows
   - CRITICAL assets highlighted in red (A-02, A-04, A-05, A-06)
   - The advisory badge at the top
   - The data health badge (as-of timestamp)
5. Save as: demo/screenshots/01-ranked-risk-queue.png
   (delete this .txt placeholder once the .png is added)
